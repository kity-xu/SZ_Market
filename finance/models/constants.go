package models

// App Setting
//---------------------------------------------------------------------------------
const (
	APP_NAME    = "market_finance"
	APP_VERSION = "0.0.2.0"
	APP_PID     = "market_finance"
)

// Context and Session Categorys
// --------------------------------------------------------------------------------

const (
	CONTEXT_END_DATE = "enddate" // 起始时间（默认当前时间）
	CONTEXT_COUNT    = "count"   // 条数（默认10条）
	CONTEXT_SCODE    = "scode"   // 证卷代码
)
